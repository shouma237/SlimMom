const path = require("path");

module.exports = {
  // Other Webpack settings...
  resolve: {
    extensions: [".js", ".jsx", ".json"],
    fullySpecified: false, // This disables the need for fully specified imports
  },
  // Other Webpack settings...
};
